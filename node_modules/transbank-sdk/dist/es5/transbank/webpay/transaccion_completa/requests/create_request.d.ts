import RequestBase from '../../../common/request_base';
declare class CreateRequest extends RequestBase {
    buyOrder: string;
    sessionId: string;
    amount: number;
    cvv: number | undefined;
    cardNumber: string;
    cardExprationDate: string;
    constructor(buyOrder: string, sessionId: string, amount: number, cvv: number | undefined, cardNumber: string, cardExpirationDate: string);
    toJson(): string;
}
export { CreateRequest };
