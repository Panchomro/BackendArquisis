import RequestBase from '../../../common/request_base';
declare class CreateRequest extends RequestBase {
    buyOrder: string;
    sessionId: string;
    amount: number;
    returnUrl: string;
    constructor(buyOrder: string, sessionId: string, amount: number, returnUrl: string);
    toJson(): string;
}
export { CreateRequest };
