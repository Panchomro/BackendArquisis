import RequestBase from '../../../common/request_base';
import TransactionDetail from '../../common/transaction_detail';
declare class MallCreateRequest extends RequestBase {
    buyOrder: string;
    sessionId: string;
    details: Array<TransactionDetail>;
    returnUrl: string;
    constructor(buyOrder: string, sessionId: string, returnUrl: string, details: Array<TransactionDetail>);
    toJson(): string;
}
export { MallCreateRequest };
