import RequestBase from '../../common/request_base';
declare class StatusRequest extends RequestBase {
    token: string;
    constructor(token: string);
    toJson(): string;
}
export { StatusRequest };
