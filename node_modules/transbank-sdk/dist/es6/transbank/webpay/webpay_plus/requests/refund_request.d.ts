import RequestBase from '../../../common/request_base';
declare class RefundRequest extends RequestBase {
    amount: number;
    constructor(token: string, amount: number);
    toJson(): string;
}
export { RefundRequest };
