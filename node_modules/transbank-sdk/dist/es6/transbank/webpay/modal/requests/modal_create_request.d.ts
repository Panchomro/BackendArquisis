import RequestBase from '../../../common/request_base';
declare class ModalCreateRequest extends RequestBase {
    buyOrder: string;
    sessionId: string;
    amount: number;
    constructor(buyOrder: string, sessionId: string, amount: number);
    toJson(): string;
}
export { ModalCreateRequest };
