import RequestBase from '../../../common/request_base';
import TransactionDetail from '../../common/transaction_detail';
declare class MallCreateRequest extends RequestBase {
    buyOrder: string;
    sessionId: string;
    cvv: number | undefined;
    cardNumber: string;
    cardExpirationDate: string;
    details: Array<TransactionDetail>;
    constructor(buyOrder: string, sessionId: string, cvv: number | undefined, cardNumber: string, cardExpirationDate: string, details: Array<TransactionDetail>);
    toJson(): string;
}
export { MallCreateRequest };
