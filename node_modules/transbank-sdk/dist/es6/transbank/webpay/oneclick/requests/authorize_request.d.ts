import RequestBase from '../../../common/request_base';
import TransactionDetail from '../../common/transaction_detail';
declare class AuthorizeRequest extends RequestBase {
    userName: string;
    tbkUser: string;
    buyOrder: string;
    details: Array<TransactionDetail>;
    constructor(userName: string, tbkUser: string, buyOrder: string, details: Array<TransactionDetail>);
    toJson(): string;
}
export { AuthorizeRequest };
