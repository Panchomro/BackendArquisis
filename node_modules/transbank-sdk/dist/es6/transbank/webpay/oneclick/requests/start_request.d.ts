import RequestBase from '../../../common/request_base';
declare class StartRequest extends RequestBase {
    userName: string;
    email: string;
    responseUrl: string;
    constructor(userName: string, email: string, responseUrl: string);
    toJson(): string;
}
export { StartRequest };
