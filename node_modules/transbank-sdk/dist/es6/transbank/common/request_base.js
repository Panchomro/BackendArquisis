class RequestBase {
    constructor(endpoint, method) {
        this.endpoint = endpoint;
        this.method = method;
    }
    toJson() {
        return JSON.stringify({});
    }
}
export default RequestBase;
